# Integration Validation & Runtime Testing Guide\n\n## 🎯 Purpose\n\nThis guide validates that all 24 product gap implementations (24 features across 8 categories) actually work together in real-time, not just in isolation.\n\n**Key Question Each Test Answers**: \"Does system X actually use state from system Y and produce expected output?\"\n\n---\n\n## TEST SUITE 1: Quest Completion Integration\n\n### Test 1.1: XP Flow Through Decay System\n\n**Setup**:\n1. Open DevTools Console\n2. Complete a quest with difficulty=3\n3. Wait 7 days (or simulate by changing `lastPracticed` in localStorage)\n\n**Expected Flow**:\n```\n✓ Quest completion triggered\n✓ XP calculated: 30 base × 1.2 difficulty × ... = 50 XP\n✓ Skill mastery increased: 40 → 45\n✓ Decay system checks: 7 days inactive → mastery drops 20%\n✓ Recovery system checks: No freeze token → streak resets\n✓ New XP cap applied if >= 1000 today\n✓ State validated: all invariants pass\n```\n\n**Verification**:\n```javascript\n// In console:\nconst state = JSON.parse(localStorage.getItem('lifequest_state'));\nconst skill = state.skillEngine.progressMap['dil-kelime'];\nconsole.log({\n  mastery: skill.mastery,        // Should be ~40-50\n  lastDecayCheck: skill.lastDecayCheck, // Should be recent\n  decayRate: skill.decayRate,    // Should be 0.05-0.15\n});\n```\n\n**Success Criteria**:\n- [ ] Mastery increased from quest\n- [ ] Decay applied if 7+ days inactive\n- [ ] State validator reports no errors\n- [ ] Dashboard shows \"XP Gained\" feedback\n\n---\n\n### Test 1.2: Recovery Token Flow\n\n**Setup**:\n1. User misses 2 days of quests (lastQuestCompletedAt is 2 days ago)\n2. Complete a quest\n3. Check if recovery bonus was applied\n\n**Expected Flow**:\n```\n✓ Days since quest calculated: 2 days\n✓ Streak reset: 5 → 1\n✓ Comeback bonus calculated: 2 days × 10% + 25% = 45% bonus\n✓ XP increased: 50 × 1.45 = 72.5 XP\n✓ Recovery notification shown\n✓ Widget updated with streak=1\n```\n\n**Verification**:\n```javascript\nconst result = ctx.completionResult; // from AppContext\nif (result) {\n  console.log({\n    xpEarned: result.xpEarned,      // Should be ~70\n    notifications: result.notifications, // Should include comeback\n    stateSnapshot: result.stateSnapshot,\n  });\n}\n```\n\n**Success Criteria**:\n- [ ] Streak properly reset\n- [ ] Comeback bonus applied to XP\n- [ ] Recovery message shown\n- [ ] Widget streak updated\n\n---\n\n### Test 1.3: Level Up → Milestone Reward\n\n**Setup**:\n1. User at level 4, needs 400 XP to reach level 5\n2. Complete quest that awards 400+ XP\n3. Check for milestone reward\n\n**Expected Flow**:\n```\n✓ Total XP reaches threshold\n✓ Level increases: 4 → 5\n✓ Milestone check: level 5 found\n✓ Reward granted: \"Skilled Learner\" title + analytics feature unlock\n✓ Level up notification shown\n✓ Widget updated with new level\n```\n\n**Verification**:\n```javascript\nconst milestone = ctx.completionResult?.leveledUp;\nconsole.log({\n  leveledUp: milestone,\n  newLevel: ctx.completionResult?.newLevel,\n  notifications: ctx.completionResult?.notifications,\n});\n```\n\n**Success Criteria**:\n- [ ] Level increased correctly\n- [ ] Milestone reward notification shown\n- [ ] No duplicate rewards\n- [ ] Analytics tracking recorded\n\n---\n\n## TEST SUITE 2: Daily Loop + Task Prioritization\n\n### Test 2.1: Daily Plan Generation\n\n**Setup**:\n1. App startup (or clear daily plan: `localStorage.removeItem('lifequest_daily_plan_v1')`)\n2. Observe if daily plan is auto-generated\n\n**Expected Flow**:\n```\n✓ getDailyPlan() returns null (first time)\n✓ generateDailyPlan() called with available quests\n✓ Quests distributed to morning/midday/evening slots\n✓ Priority scoring applied: urgency + effort + dopamine\n✓ Plan saved to localStorage\n✓ Dashboard displays focus quest for current time slot\n```\n\n**Verification**:\n```javascript\nconst plan = JSON.parse(localStorage.getItem('lifequest_daily_plan_v1'));\nconsole.log({\n  date: plan.date,\n  morning: plan.morning.length,  // Should be 2-3\n  midday: plan.midday.length,    // Should be 2-3\n  evening: plan.evening.length,  // Should be 2-3\n  total: plan.total,\n});\n```\n\n**Success Criteria**:\n- [ ] Daily plan created on first load\n- [ ] Quests properly distributed to slots\n- [ ] No duplicate quests in plan\n- [ ] Plan date matches today\n- [ ] Reset happens next day (check at midnight)\n\n---\n\n### Test 2.2: Priority Scoring Algorithm\n\n**Setup**:\n1. Get daily plan\n2. Check which quest appears \"first\" (highest priority)\n3. Verify priority score calculation\n\n**Expected Flow**:\n```\n✓ Priority score calculated for each quest\n✓ Scoring components:\n  - Urgency (high=40, normal=20, low=5)\n  - Mastery weakness (40 - mastery)\n  - Difficulty (difficulty × 5)\n  - Effort/reward ratio: (dopamine / effortRequired) × 15\n✓ Overdue quests have highest urgency\n✓ Weak skills prioritized\n```\n\n**Verification**:\n```javascript\nconst quests = ctx.quests;\nconst scored = quests.map(q => ({\n  title: q.title,\n  priorityScore: q.priorityScore || 0,\n  urgency: q.urgency,\n  effortRequired: q.effortRequired,\n})).sort((a,b) => b.priorityScore - a.priorityScore);\nconsole.table(scored);\n```\n\n**Success Criteria**:\n- [ ] Overdue quests ranked highest\n- [ ] Weak skills preferred\n- [ ] High-effort quests in midday slot\n- [ ] Low-effort quests in evening slot\n\n---\n\n## TEST SUITE 3: Notification System\n\n### Test 3.1: Smart Notification Frequency\n\n**Setup**:\n1. Complete a quest (trigger notification)\n2. Dismiss/ignore the notification multiple times\n3. Track if frequency adapts\n\n**Expected Flow**:\n```\n✓ First notification sent: frequency = \"normal\"\n✓ User ignores: trackNotificationIgnored(skillId) called\n✓ After 3 ignores: frequency = \"low\"\n✓ After 5 ignores: frequency = \"paused\"\n✓ nextEligible timestamp increases with each ignore\n✓ Engagement resets counter\n```\n\n**Verification**:\n```javascript\nconst freqMap = JSON.parse(localStorage.getItem('lifequest_notify_freq_v1'));\nObject.entries(freqMap).forEach(([skillId, freq]) => {\n  console.log({\n    skillId,\n    frequency: freq.frequency,\n    ignoreCount: freq.ignoreCount,\n    nextEligible: freq.nextEligible,\n  });\n});\n```\n\n**Success Criteria**:\n- [ ] Frequency decreases after ignores\n- [ ] Frequency increases after engagement\n- [ ] nextEligible timestamp respected\n- [ ] No notifications after \"paused\" until reset\n\n---\n\n### Test 3.2: Inactivity Detection\n\n**Setup**:\n1. Don't complete any quest for 2 days\n2. Check if inactivity notification triggers\n\n**Expected Flow**:\n```\n✓ detectInactivity() checks lastQuestCompletedAt\n✓ Days inactive calculated\n✓ If > 1 day: isInactive = true\n✓ Inactivity message generated\n✓ Retention hook notification queued\n```\n\n**Verification**:\n```javascript\nconst state = JSON.parse(localStorage.getItem('lifequest_state'));\nconst lastQuestDate = new Date(state.user.lastQuestCompletedAt);\nconst daysInactive = (Date.now() - lastQuestDate.getTime()) / (1000*60*60*24);\nconsole.log({ daysInactive, isInactive: daysInactive > 1 });\n```\n\n**Success Criteria**:\n- [ ] Inactivity detected at 1+ days\n- [ ] Retention message shown\n- [ ] Recovery bonus eligible\n- [ ] Streak risk updated\n\n---\n\n## TEST SUITE 4: Widget & Native Sync\n\n### Test 4.1: Widget Cache Consistency\n\n**Setup**:\n1. Complete a quest\n2. Immediately check widget cache\n3. Complete another quest\n4. Verify cache updated\n\n**Expected Flow**:\n```\n✓ Quest complete → orchestrateQuestCompletion()\n✓ Phase 7: updateWidgetCache() called\n✓ Widget cache written to localStorage\n✓ Cache contains: focusSkillId, mastery, streak, risk level\n✓ Widget reads cache, renders current state\n✓ No stale data in widget\n```\n\n**Verification**:\n```javascript\nconst cache1 = JSON.parse(localStorage.getItem('lifequest_widget_cache_v1'));\nconsole.log('Cache after quest 1:', cache1);\n\n// Complete another quest\n\nconst cache2 = JSON.parse(localStorage.getItem('lifequest_widget_cache_v1'));\nconsole.log('Cache after quest 2:', cache2);\nconsole.assert(cache2.streak === cache1.streak + 1, 'Streak should increment');\n```\n\n**Success Criteria**:\n- [ ] Cache updated after each quest\n- [ ] Streak in cache matches AppState\n- [ ] Mastery in cache matches skill progress\n- [ ] lastUpdate timestamp recent\n\n---\n\n### Test 4.2: Native Sync Fallback\n\n**Setup**:\n1. On Android: Complete quest, check Capacitor Preferences\n2. On Web: Complete quest, verify localStorage used as fallback\n\n**Expected Flow (Android)**:\n```\n✓ Phase 9: orchestrateQuestCompletion → syncToNative()\n✓ Capacitor.Preferences.set('lifequest_skills_v1', JSON.stringify(payload))\n✓ Capacitor.Filesystem.writeFile(filesDir/lifequest_skills_v1.json)\n✓ recordSync('preferences') + recordSync('filesystem')\n✓ Native widget can read preferences\n```\n\n**Expected Flow (Web)**:\n```\n✓ syncToNative() attempts import\n✓ Capacitor not available → catch block\n✓ localStorage already has data\n✓ graceful degradation → no error\n```\n\n**Verification**:\n```javascript\n// Android:\nconst { Preferences } = await Capacitor.Preferences;\nconst data = await Preferences.get({ key: 'lifequest_skills_v1' });\nconsole.log('Native data:', data);\n\n// Web:\nconst syncLog = JSON.parse(localStorage.getItem('lifequest_last_sync_v1'));\nconsole.log('Sync log:', syncLog);\n```\n\n**Success Criteria**:\n- [ ] Android: Preferences and Filesystem both have data\n- [ ] Web: No crash, fallback to localStorage\n- [ ] Data consistent across storage layers\n- [ ] Next app open reads correctly\n\n---\n\n## TEST SUITE 5: Game Economy\n\n### Test 5.1: XP Soft Cap\n\n**Setup**:\n1. User at level 30 (pre-soft-cap), complete 5 quests\n2. User at level 55 (post-soft-cap), complete 5 quests\n3. Compare XP requirements\n\n**Expected Flow**:\n```\n✓ Level 30: xpRequired = 3000\n✓ Level 55: xpRequired = base × (1.1)^5 = base × 1.61\n✓ Soft cap makes progression slower at high levels\n✓ Still achievable but incentivizes rest days\n```\n\n**Verification**:\n```javascript\nimport { calculateXpForLevel } from './hooks/useGameEconomy';\nconsole.log({\n  level30: calculateXpForLevel(30),\n  level50: calculateXpForLevel(50),\n  level55: calculateXpForLevel(55),\n  level60: calculateXpForLevel(60),\n});\n```\n\n**Success Criteria**:\n- [ ] XP requirement increases with level\n- [ ] Soft cap applies at level 50+\n- [ ] Diminishing returns visible at high levels\n- [ ] Not impossible to progress\n\n---\n\n### Test 5.2: Milestone Reward Chain\n\n**Setup**:\n1. Skip to level 5 (via localStorage edit)\n2. Complete quest to trigger level 6+\n3. Check if reward unlocked\n\n**Expected Flow**:\n```\n✓ Level 5: \"Adventurer I\" title\n✓ Level 10: Analytics feature unlock\n✓ Level 25: Recovery token grant\n✓ Level 50: Premium theme unlock\n✓ Rewards persist across app reload\n```\n\n**Verification**:\n```javascript\nconst milestones = getMilestones(ctx.user.level);\nconst unlocked = milestones.filter(m => m.unlocked);\nconsole.table(unlocked);\n```\n\n**Success Criteria**:\n- [ ] Each level has corresponding milestone\n- [ ] Reward granted exactly once\n- [ ] Reward persists in localStorage\n- [ ] No duplicate grants on refresh\n\n---\n\n## TEST SUITE 6: State Validation\n\n### Test 6.1: Invariant Enforcement\n\n**Setup**:\n1. Manually corrupt localStorage: set user.level = 150\n2. Complete a quest\n3. Check if validator catches it\n\n**Expected Flow**:\n```\n✓ validateAppState() runs after mutation\n✓ Detects: level 150 > 100 (out of range)\n✓ Returns errors: ['Invalid user level: 150']\n✓ attemptStateRecovery() clamps: level = 100\n✓ Recovered state replaces current\n✓ Console logs all fixes\n```\n\n**Verification**:\n```javascript\n// Corrupt state:\nconst state = JSON.parse(localStorage.getItem('lifequest_state'));\nstate.user.level = 150;\nlocalStorage.setItem('lifequest_state', JSON.stringify(state));\n\n// Next quest completion:\nconst validation = validateAppState(state);\nconsole.log({\n  isValid: validation.isValid,\n  errors: validation.errors,\n});\n```\n\n**Success Criteria**:\n- [ ] Validator detects out-of-bounds values\n- [ ] Recovery clamps to valid range\n- [ ] No exception thrown\n- [ ] State fully recoverable\n\n---\n\n## TEST SUITE 7: Retention Hooks\n\n### Test 7.1: Loss Aversion Messaging\n\n**Setup**:\n1. User has 5-day streak\n2. Complete quest next day (day 6)\n3. Check messaging\n\n**Expected Flow**:\n```\n✓ getLossAversionMessage(5) returns:\n  \"🔥 5 günlük güzel streak'i kaybetmeyin!\"\n✓ Message emphasizes LOSS not GAIN\n✓ Near-miss effects active (< 3 days to milestone)\n✓ Notification sent if user inactive 1+ day\n```\n\n**Verification**:\n```javascript\nimport { getLossAversionMessage } from './utils/retentionEngine';\nconst msg = getLossAversionMessage(5);\nconsole.log(msg);\n```\n\n**Success Criteria**:\n- [ ] Loss-aversion language used\n- [ ] Streak number shown in message\n- [ ] Message triggers near-miss bonus\n- [ ] Psychological hook applied\n\n---\n\n## TEST SUITE 8: Analytics Flow\n\n### Test 8.1: Event Tracking Pipeline\n\n**Setup**:\n1. Enable analytics (in utils/analyticsEngine.ts)\n2. Complete 3 quests\n3. Check analytics event log\n\n**Expected Flow**:\n```\n✓ trackEvent('quest_completed', properties) called\n✓ Event stored in localStorage\n✓ Properties include: questId, skillId, xp, streak, level, difficulty\n✓ Last 1000 events kept (old purged)\n✓ Can calculate funnel: start → focus → quest → habit\n```\n\n**Verification**:\n```javascript\nconst events = JSON.parse(localStorage.getItem('lifequest_analytics_events_v1'));\nconst completed = events.filter(e => e.name === 'quest_completed');\nconsole.log(`Tracked ${completed.length} quest completions`);\nconsole.table(completed.map(e => e.properties).slice(0, 3));\n```\n\n**Success Criteria**:\n- [ ] Events recorded for each quest\n- [ ] Funnel metrics calculable\n- [ ] No event loss (< 1000 events)\n- [ ] Timestamps accurate\n\n---\n\n## 🔥 CRITICAL RUNTIME VALIDATION\n\n### Checklist: After Each Major Feature\n\n```javascript\n// Paste in console after completing a quest:\n\nconst state = JSON.parse(localStorage.getItem('lifequest_state'));\nconst result = ctx.completionResult; // if inside React component\n\nconsole.log('=== RUNTIME VALIDATION CHECK ===' );\nconsole.log('✓ Quest completed?', result?.xpEarned > 0);\nconsole.log('✓ State valid?', isStateHealthy(state));\nconsole.log('✓ XP increased?', state.user.xp > 0);\nconsole.log('✓ Skill updated?', Object.keys(state.skillEngine?.progressMap).length > 0);\nconsole.log('✓ Widget cached?', !!localStorage.getItem('lifequest_widget_cache_v1'));\nconsole.log('✓ Event tracked?', JSON.parse(localStorage.getItem('lifequest_analytics_events_v1')).length > 0);\nconsole.log('✓ Notifications prepared?', result?.notifications?.length >= 0);\nconsole.log('✓ Dashboard feedback visible?', !!result?.stateSnapshot);\n```\n\n---\n\n## 📊 Expected Test Results\n\n**If all tests pass**: ✅ **System is production-ready**\n- All 24 features integrated\n- No race conditions\n- State consistency maintained\n- UX feedback working\n- Native sync functioning\n\n**If some tests fail**: ⚠️ **Debug using EVENT_ORDER.md**\n1. Check which phase failed\n2. Verify that phase's dependencies\n3. Fix orchestrator order if needed\n4. Re-run test\n\n---\n\n**Last Updated**: 2026-06-21\n